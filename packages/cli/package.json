{
  "name": "@mcp-pm/cli",
  "version": "1.0.0",
  "description": "CLI for MCP Project Manager - init, start, stop, status",
  "type": "module",
  "bin": {
    "mcp-pm": "dist/index.js"
  },
  "main": "dist/index.js",
  "files": ["dist/"],
  "scripts": {
    "build": "tsup src/index.ts --format esm --clean",
    "dev": "tsup src/index.ts --format esm --watch",
    "lint": "tsc --noEmit",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "commander": "^12.1.0",
    "chalk": "^5.3.0",
    "ora": "^8.1.0"
  },
  "devDependencies": {
    "tsup": "^8.3.0",
    "typescript": "^5.7.0",
    "@types/node": "^22.0.0"
  }
}
